PROJECT_ROOT=$(dirname $(readlink -f $0))/../..

AZURE_PROBLEM_DIR=/home/azureuser/cloudfiles/code/te_problems
ALL_PROBLEM_LIST=$(cd $AZURE_PROBLEM_DIR; ls)

INPUT_DIR=${PROJECT_ROOT}/input
OUTPUT_DIR=${PROJECT_ROOT}/output

TEAL_SCRIPT=${PROJECT_ROOT}/run/teal.py
LP_SCRIPT=${PROJECT_ROOT}/run/lp.py
SPACETE_SCRIPT=${PROJECT_ROOT}/run/spaceTE.py

IRIDIUM_ADAPTER_SCRIPT=${PROJECT_ROOT}/utils/iridium/adapter.py
